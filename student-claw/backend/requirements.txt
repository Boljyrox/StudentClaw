# ─── Student Claw — backend dependencies ───
# Python 3.11 or 3.12 required (python-telegram-bot v20 is incompatible with 3.13+).

# Web / API
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0        # loads backend/.env (see app/__init__.py)

# Database (async)
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29

# Telegram bot
python-telegram-bot>=20,<21

# Agnes AI (OpenAI-compatible) + RAG
openai>=1.40
qdrant-client>=1.7
tiktoken>=0.7

# Object storage + job queue
minio>=7.2
redis>=5.0

# Multi-modal parsing / OCR
PyMuPDF>=1.24            # PDF text extraction (imported as `fitz`)
python-pptx>=0.6        # PPTX slide extraction
Pillow>=10.0
langdetect>=1.0.9
tenacity>=8.2            # Retry logic for VLM API calls
fastembed>=0.3          # Local embeddings (Agnes has no embeddings endpoint)

# Auth / crypto / HTTP
bcrypt>=4.1
PyJWT>=2.8
cryptography>=42.0
httpx>=0.26.0,<0.27.0

# Bill splitting — PayNow QR generation
qrcode[pil]>=7.4
